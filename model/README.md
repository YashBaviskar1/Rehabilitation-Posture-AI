### Model Stuff

make sure whie adding files in backend branch to

```bash
git checkout model-branch

git add model/

git commit -m "Describe your changes here"

# Push to the backend branch
git push origin model-branch
```
